[package]
name = "synth"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = { version = "0.30", default-features = false }
rustysynth = "1"
terminal-games-sdk = { git = "https://github.com/terminal-games/terminal-games.git" }
